import 'package:flutter/material.dart';

void main() {
  runApp(const PlansToDo());
}

class PlansToDo extends StatelessWidget {
  const PlansToDo({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(),
    );
  }
}
